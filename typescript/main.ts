function toGreet(str: string): string {
  return `Hello, ${str}`;
}

console.log(toGreet("World"));

// Grasshoper - Basic Function Fixer
const addFive = (num: number): number => {
  let total = num + 5;
  return total;
};
